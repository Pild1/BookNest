{
  "name": "booknest-assignment-4-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "db:migrate": "node prisma/runMigrations.js",
    "db:seed": "node prisma/seed.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@prisma/client": "^6.8.0",
    "selfsigned": "^2.4.1"
  },
  "devDependencies": {
    "prisma": "^6.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
